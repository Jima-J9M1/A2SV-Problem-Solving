

    size = int(input())
     
    arr = list(map(int,input().split()))
     
    arr.sort(reverse=True)
     
    if arr[0] >= arr[1] + arr[2]:
        print("NO")
    else:
        print("YES")
        arr[0], arr[1] = arr[1], arr[0]
        print(*arr)
     
     
        
     
